import React from "react";
import FormGenerator from "../(components)/form-generator";

const CreatePage = () => {
	const isUpdate = false;
	return <FormGenerator isUpdate={isUpdate} />;
};

export default CreatePage;
